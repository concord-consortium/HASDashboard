radius(n)
  -webkit-border-radius n
  -moz-border-radius n
  border-radius n

vertical()
  -webkit-transform rotate(90deg)
  -moz-transform rotate(90deg)
  -ms-transform rotate(90deg)
  -o-transform rotate(90deg)
  transform rotate(90deg)
  -webkit-transform-origin: top right;
  -moz-transform-origin: top right;
  -o-transform-origin: top right;
  transform-origin: content '';

animatedSlide(delay=200ms)
  transition-duration delay
  transition-timing-function ease-in


top-openable(y, delta)
  animatedSlide()
  position fixed
  top y
  &.open
    top (y + delta)

overlay(z=10)
  box-sizing border-box
  background-color hsl(210,76,36)
  position fixed
  opacity 0.99
  z-index z
  padding 0.5em

shadow(x=1px, y=-2px, spread=2px)
  box-shadow x y spread hsla(0,0,0,0.5)

bubbleBox(color=white, spacing=0, radius=0)
  box-sizing border-box
  background-color color
  position absolute
  margin 0px
  padding spacing
  left    spacing
  right   spacing
  top     spacing
  bottom  spacing
  radius(radius, radius, radius, radius)

hidden-sliding()
  transition 500ms
  position absolute
  left 0
  top 0
  width 100%
  height 100%
  margin 4px
  box-sizing border-box
  &.hidden-left
    left -101%
  &.hidden-right
    left 101%
